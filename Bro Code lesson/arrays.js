// array = a variable like structure that can hold more than one value


let fruits = ["apple", "orange", "banana"];



// fruits.push("coconut"); // to add an element to the end of an array
// fruits.pop(); // to remove the last element of an array
// fruits.unshift("mango"); //to add element to the beginning of an array
// fruits.shift(); // to remove an element from the beginning of an array


// to get the length of an array


for (let i = 0; i < fruits.length; i++) {
    const element = fruits[i];
    console.log(element)
    
}

